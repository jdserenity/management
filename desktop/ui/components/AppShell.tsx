// src/components/AppShell.tsx — desktop nav shell (companion uses MobileAppShell variant="companion")

import MobileAppShell from '@/components/MobileAppShell';

const AppShell = () => <MobileAppShell variant="desktop" />;

export default AppShell;
